'use client';
import { FormEvent,useEffect,useState } from 'react';
import { supabase } from '@/lib/supabase';
import { StoreHeader } from '@/components/StoreHeader';
import { AccountGate } from '@/components/AccountGate';
import { AccountNav } from '@/components/AccountNav';
export default function Profile(){const[profile,setProfile]=useState<any>(null);const[email,setEmail]=useState('');const[message,setMessage]=useState('');const[busy,setBusy]=useState(false);useEffect(()=>{(async()=>{const{data:user}=await supabase.auth.getUser();setEmail(user.user?.email||'');if(!user.user)return;const{data}=await supabase.from('customers').select('*').eq('auth_user_id',user.user.id).single();setProfile(data)})()},[]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!profile)return;setBusy(true);setMessage('');const fd=new FormData(e.currentTarget);const{error}=await supabase.from('customers').update({first_name:String(fd.get('first_name')||''),last_name:String(fd.get('last_name')||''),phone:String(fd.get('phone')||'')}).eq('id',profile.id);setMessage(error?error.message:'Bilgileriniz güncellendi.');setBusy(false)}return <AccountGate><main className="store"><StoreHeader/><section className="account-shell"><AccountNav/><div className="account-main"><div className="section-head"><div><h1>Profilim</h1><p className="muted">Hesap ve iletişim bilgilerinizi düzenleyin.</p></div></div>{!profile?<div className="form-card">Yükleniyor…</div>:<form className="form-card account-profile-form" onSubmit={save}><label>E-posta<input value={email} disabled/></label><div className="form-grid two"><label>Ad<input required name="first_name" defaultValue={profile.first_name}/></label><label>Soyad<input required name="last_name" defaultValue={profile.last_name}/></label></div><label>Telefon<input name="phone" defaultValue={profile.phone||''}/></label>{message&&<div className="notice">{message}</div>}<button className="button" disabled={busy}>{busy?'Kaydediliyor…':'Bilgilerimi Kaydet'}</button></form>}</div></section></main></AccountGate>}
